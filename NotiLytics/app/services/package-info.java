/**
 * Provides core business logic and integrations for NotiLytics.
 *
 * <p>This layer communicates with external APIs, processes data,
 * and supplies results to controllers. Services are typically
 * annotated with {@code @Singleton} and injected via Guice.</p>
 */
package services;