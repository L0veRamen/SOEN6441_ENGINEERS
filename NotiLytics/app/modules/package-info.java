/**
 * Contains Google Guice dependency injection modules for configuring
 * application bindings.
 *
 * <p>These modules define how interfaces are bound to concrete
 * implementations at runtime.</p>
 */
package modules;